{
  "name": "aifrobeats",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
